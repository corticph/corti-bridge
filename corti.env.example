# Corti bearer token — required
# Used for both upstream auth and local gateway validation.
# Claude Code sends this in its Authorization header.
CORTI_BEARER="${CORTI_BEARER:-your-corti-bearer-token}"

# Gateway bind address
CORTI_HOST="${CORTI_HOST:-127.0.0.1}"
CORTI_PORT="${CORTI_PORT:-4000}"
