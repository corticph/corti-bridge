# Corti bearer token — required
CORTI_BEARER="${CORTI_BEARER:-your-corti-bearer-token}"

# Local API key — clients must send this in Authorization or x-api-key
CORTI_LOCAL_KEY="${CORTI_LOCAL_KEY:-sk-corti-local-change-me}"

# Gateway bind address
HOST="${HOST:-127.0.0.1}"
PORT="${PORT:-4000}"
